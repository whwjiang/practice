#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using ui = unsigned int;

void solve() {
  ui n, k;
  cin >> n >> k;
  vector<ui> cows(n);
  for (size_t i = 0; i < n; ++i) {
    cin >> cows[i];
  }

}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  solve();
}
